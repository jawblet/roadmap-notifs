name: hourly-cron-job
on:
  schedule:
    - cron: '*/10 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: hourly-cron-job
        run: curl -X POST "https://piano-roadmap.vercel.app/api/airtable/"
